# 12/06/2026 - 14/06/2026

### What I worked on:
Firstly, I rearranged the whole repository, making it easier to navigate by separating out the different stages (training, compiling, runtime). Added some more tests for running the compiled .hef model on a live video feed from the camera, which were successful. Created base movement handling script from template in shared repository, and made a separate python script for handling the machine learning model, separate from the script that handles all of the logic and decision making that happens during rescue. Tested Hailo's built in async mode, but I don't know if async is working or not. For debugging, I initially just used cv2.imshow to display a video output onto the Pi's display, and later changed this to save the video to a .mp4 file.

### Reflection
Yet again, Hailo's documentation wasn't the best, and I heavily relied on trial and error to make the test inference script work. Once it worked, I was afraid to touch it again, in case something broke, because I wouldn't know how to fix it.

### Decisions made
I wanted the machine learning inference to run asynchronously, meaning that it will be handling multiple (1-3) frames at once, rather than having frames queue up whilst inference is running on a frame (which takes about 20-50ms). I don't think my script is doing that right now, but I'll try to get that working later.

### Testing notes
I tested both .pt and .hef models, both displaying text outputs in the terminal and video outputs with annotations to better understand where the model's strenghts and weaknesses are. Due to the simple training of the model, it could not detect balls up really close. This will need to be fixed in the next iteration.

### AI log use
ChatGPT/Google AI for debugging whilst testing inference with the machine learning model. Getting feedback on what can be optimised and improved within my code.
