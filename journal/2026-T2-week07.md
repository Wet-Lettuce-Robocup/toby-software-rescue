# 01/06/2026

### What I worked on:
I properly set up a vision node which subscribes to the front camera publisher, and then should in theory perform inference on the image from the camera, and for testing I set up a publisher to display the image with cv2. I then tried to download the proprietary Hailo Dataflow Compiler (DFC) on my laptop, but ran in to a lot of issues with it.
I then spent a lot of time modifying the python script that the DFC will use to compile my script, and I changed quite a lot to get it to work since the original script was made with an older version of the DFC. I changed the layout of this repository to be more organised, and to separate each stage of development within the src/ directory. 

### Reflection
The largest problem this week is Hailo's proprietary software and architecture. If I wanted to run a premade model on the NPU, it would be very easy, however compiling my own model is quite complex and requires a Linux computer running x86_64 architecture, which I don't have access to at home. I tried many workarounds, including running it in a Linux vm on my laptop, installing it on the Raspberry Pi, and running it on Docker on my laptop. Emulating x86_64 architecture with Docker on my laptop would be possible, but unusably slow. For whatever reason, the only place to download Hailo's software is their website, which is extremely buggy and would only work on a Chromium based browser in an Incognito tab, and the installation process included running a shell script which would only run on Linux. The solution was to use a Windows computer running a Linux docker container, and using a workaround for the shell script (https://medium.com/@mgreiner79/how-to-run-hailo-dataflow-compiler-using-docker-on-windows-8442a0215c53 + https://pub.towardsai.net/custom-dataset-with-hailo-ai-hat-yolo-raspberry-pi-5-and-docker-0d88ef5eb70f)
I had to modify both the Dockerfile to make it more tailored for my use, including caching downloads so it doesn't take 40 minutes on each compile, and mounting volumes so that I can change the python script without having to rebuild the Docker container.

### Decisions made
Hailo Dataflow Compiler only works in an Ubuntu environment on a x86_64 architecture computer with at least 16GB of ram, hence I had to use a Windows computer running Docker in order to compile my ml model into a .hef file.
I used a Docker container to run the DFC, for easier portability and modularity, so that it can run without affecting the computer's files. In hindsight, I could have just used the Windows Subsystem for Linux (WSL2) and installed Ubuntu onto the PC, avoiding all of my problems with Docker, but I didn't want to have the DFC interact with any other files on the computer, since it is the school's computer.

### Testing notes
Nothing notable

### AI log use
ChatGPT for debugging hailo script
