To build base image:
* Requires x86_64 operating system, Docker, minimum 16GB memory, and minimum 50GB of available storage

`docker build -t hailo-base:latest .`


Then to run the actual image:
`docker compose up`